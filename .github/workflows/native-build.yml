name: NativeBuild

on:
  push:
    branches: [ main, jdk25, fxml ]
    tags:
      - '*'

env:
  APP_NAME: java-demo

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        # os: [ubuntu-latest, windows-latest, macos-latest]
        include:
          - os: ubuntu-latest
            architecture: x86_64-linux
            executeable-extension: 
          # - os: windows-latest 
          #   architecture: x86_64-windows
          #   executeable-extension: .exe
          - os: macos-latest
            architecture: x86_64-darwin
            executeable-extension: 
    steps:
    - if: matrix.os == 'ubuntu-latest'
      name: Get OS info
      run: uname -a

    - uses: actions/checkout@v4.1.2
      name: Checkout the repository

    - if: matrix.os == 'ubuntu-latest'
      name: check if musl based
      run: ldd --version

    - uses: graalvm/setup-graalvm@v1.4.3
      with:
        distribution: 'liberica'
        java-version: '25'
        java-package: 'jdk+fx'
        github-token: ${{ secrets.GITHUB_TOKEN }}
        cache: gradle

    # - if: matrix.os == 'macos-latest'
    #   name: Install xcode
    #   run: |
    #     xcode-select --install

    - if: matrix.os == 'ubuntu-latest'
      name: Install libraries
      run: |
          sudo apt update
          sudo apt install libasound2-dev libavcodec-dev libavformat-dev libavutil-dev libgl-dev libgtk-3-dev libpango1.0-dev libxtst-dev

    - if: matrix.os == 'windows-latest'
      name: Add msbuild to PATH
      uses: microsoft/setup-msbuild@v2.0.0
    - if: matrix.os == 'windows-latest'
      name: Visual Studio shell
      uses: egor-tensin/vs-shell@v2.1

    - name: Printing GRAALVM Versions
      run: |
        echo "GRAALVM_HOME: $GRAALVM_HOME"
        echo "JAVA_HOME: $JAVA_HOME"
        java --version
        native-image --version
        ./gradlew --version

    - if: matrix.os == 'ubuntu-latest' || matrix.os == 'macos-latest'
      name: check for libawt in java installation folder
      run: |
        find $GRAALVM_HOME -name "libawt*"

    - if: matrix.os == 'windows-latest'
      name: Temporarily fix gradle tmpdir
      run: |
        echo "Current working directory: $pwd"
        type gradle.properties
        echo "org.gradle.jvmargs=-Djava.io.tmpdir=D:/Temp" >> gradle.properties
        type gradle.properties
        mkdir D:/Temp
        ls D:/Temp

    - name: Build native client
      run: ./gradlew -s nativeCompile

    - name: See build nativeCompilation folders for architecture
      run: ls -l build/native/nativeCompile

    - if: matrix.os == 'ubuntu-latest' || matrix.os == 'macos-latest'
      name: check for libawt in build folder
      run: |
        find build/ -name "libawt*"
      
    - name: Make dist directory
      run: mkdir dist

    - name: Copy native client to dist
      run: cp -r build/native/nativeCompile/${{ env.APP_NAME }}${{ matrix.executeable-extension }} dist
      
    - name: Upload
      uses: actions/upload-artifact@v4.3.1
      with:
        name: ${{ env.APP_NAME }}-${{ matrix.os }}
        path: dist